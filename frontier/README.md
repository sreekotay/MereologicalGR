# Frontier — PB-2 / capacity-interval exploration

Captured generative exploration around PB-2: the measure-on-ordering, the ordering/adjacency
"capacity-interval," and the black-hole fusion/un-fusion picture. **This is not canon.** It imports
external machinery (Wald entropy, modular flow, QES/islands, de Sitter thermodynamics) as scaffolding
the corpus does not own, and it references freely. Survivors get audited and graduated into the
canonical notes; the rest stays here for the record or is rejected.

**This README is the only living document in the folder.** The three notes below are frozen snapshots
— captured trains of thought, never back-patched.

## Files

| File | Role | Status |
|---|---|---|
| `pb2-capacity-interval.md` | The spine — the full 18-section thread (selector vs measured → legs → Wick rotation → Wald fusion → Page un-fusion → Planck wall → cosmology) | FROZEN snapshot |
| `thrust-formation.md` | Birth side: collapse as the fusion event (charge pins to area; trapped ⇒ singular; no reverse-Page) | FROZEN snapshot |
| `thrust-cosmological-mirror.md` | One Planck wall, two sides: BH endpoint vs Big Bang; dS floor; arrow as traverse | FROZEN snapshot |

The earlier `thrust-ink-core.md` triage has been folded into this README and removed — its job
(identify survivors and where they land) is done and recorded below.

## What graduated into canon

The lineage-clean core that survived audit and is now inked — none of it needs the modular / Wald /
Page machinery:

```text
A1  measure is intensive / per-type, not c-like          → CLAIMS PB-2 row
A2  earn-condition = units + bound + rate; two-sided bet   → CLAIMS PB-2 row + D12
A3  ordering-specificity lives in the EXTENSIVE amount      → CLAIMS PB-2 row
b4 §6a  per-adjacency corner forces two signatures (exported, not tested there)
PB-2.1  non-substitutability — depth not width            → adversarial test a4 §6
PB-2.2  flow-freeness — count not duration                 → favorable test b9
b9      many-body Lieb-Robinson cone — first POSITIVE witness for PB-1/2, GR-free
A4      influence = shared hinge of gravity & information   → CLAIMS grammar row
formation §4  trapped ⇒ ordering-collapse (horizon → singularity, GR/ħ-free) → b4 §2
C1/C2   Wald fused limit + Page un-fusion                  → b4 §6b (graded contact: imported,
        could-fail, NOT owned — explains the §6 corner as the stationary/fused limit)
```

**The landing came in below the modular route.** Spine §16 nominated the Wald/modular/Page core as
"the inkable core." What actually inked first imports none of it — b9 is just depth-vs-width,
layer-count, and ceiling-vs-front on a lattice with the metric absent.

## Live front — inked as graded contacts (b4 §6b); gates remain to go contact → owned

C1 and C2 now have a canonical home: **b4 §6b** inks them as IMPORTED, could-fail contacts (the Wald
fused limit + the Page un-fusion), which explain the §6 per-adjacency corner as the stationary/fused
limit. They are **not owned** — the gates below are what would promote them contact → owned.

```text
C1  Wald fusion:
    horizon entropy = Noether charge of the horizon-generating boost = A/4 (Wald), read as the
    ordering-charge fused to the adjacency-area at a stationary horizon.
    GATE: the boost = ordering-symmetry identification must be FORCED by the grammar, not chosen to
    fit S = A/4 — then stable under higher-curvature Wald terms, or charge ≠ area beyond GR turned
    into a predictable test (spine seam S1).

C2  Page un-fusion:
    S_rad = min(ordering-accounted, adjacency-area); pre-Page ordering-dominated (the per-ordering
    home), post-Page adjacency-dominated.
    GATE: "rising branch = ordering leaving" earned beyond interpretation (S5); no required stable
    remnant (S3); pre-Page irreducible-to-area survives (S2).
```

Deeper birth-side detail (collapse as the fusion event) stays frozen in `thrust-formation.md`; only
C1's stationary core landed in b4 §6b.

## Parked — real contacts, lower tier

Lean harder on imports; lower value than the live front. (C4's home already exists; C3/C5/C6 would
need a new vehicle.)

```text
C3  modular flow = ordering symmetry; QES = replica Z_n fixed point.
    GATE: bound to where modular = boost is a theorem (Bisognano–Wichmann); the selector mode
    graduates only if QES extremization carries irreducible causal-access content, not a generic
    geometric extremum (spine seams S4, S8).

C4  dE = T dS hinge — T = dE/dS as the intensive conjugate (home b6 / D5).
    GATE: keep the owned residue (A4, below) separate from the thermodynamic naming; graduates only
    if the conjugate T = dE/dS is FORCED by the compositions, not fitted. (T = κ/2π is already an
    imported b4/b6 scalar.)

C5  internal framelessness / third law (T = 0 ⇒ no clock).
    GATE: promote T = 0 ⇒ no-tick from analogy to mechanism via the quantum speed limit, and exhibit
    an actual transformation law for the "internal speed limit." Else parks.

C6  de Sitter floor (Λ > 0 ⇔ T_dS > 0 ⇔ S_dS < ∞; heat death ≠ absolute zero).
    GATE: graduates only after the capacity/leg invariant is itself owned; lowest priority.
```

## Un-excavated — structural ideas not yet lifted (candidate next digs)

Not contacts; structural moves still sitting in the frozen snapshots. The first three are the
low-import front — owned or projected, cheap to ink.

```text
LOW-IMPORT FRONT — now cleared:
  A4           → GRADUATED to CLAIMS grammar row (influence = shared hinge of gravity & information).
  formation §4 → GRADUATED to b4 §2 (trapped ⇒ ordering-collapse, horizon → singularity; the
                 loss-edge / stranding bet stays open).
  A5           → AUDITED OUT, stays parked. The projection that legitimately exchanges under boost
                 and coincides at the null limit is (flow-face, adjacency-face) — already in a2 §3.
                 "Ordering ↔ adjacency exchange" conflates A0's INVARIANT ordering (causal
                 precedence, boost-invariant) with frame-dependent coordinate time-order of
                 spacelike pairs (which have no invariant ordering at all). The only owned residue
                 (null limit = framelessness pole) is already in a2 §3 + §8. Reviving A5 would
                 require extending A0's definition of ordering — grammar work at the root, not a dig.

SUB-CLAIMS buried under the live front (C1/C2), never separately lifted:
  formation §3  apparent vs event horizon; the exact fusion operator is RETROACTIVE (Wald exact only
                in the stationary limit). The thrust's sharpest new seam (F1).
  formation §5  the ħ-asymmetry: fusion classical (t ~ M), un-fusion quantum (t ~ M³/ħ); no
                classical un-fusion at all.
  formation §6  no reverse-Page; collapse ≠ time-reverse of evaporation (a step, not a curve).
  formation §7  no-hair as concealment, not loss (soft / quantum hair = the could-fail).
  spine §1      selector vs measured — a clean conceptual tool, folded into C3 but never treated.

OTHER net-new conjecture:
  mirror §5     window-width / "coherence budget" as a single cosmological invariant (no derivation).
```

## Not actively tracked

The most speculative leaps — kept in the frozen spine/thrusts for the record, dropped from the live
map. Most carry KILL / relabel flags by their own seams:

```text
rest-capacity invariant (the m²-analog)        spine §5   — no transformation law exhibited
Wick rotation / three-temperatures-as-one      spine §7,§9 — single-mode only (seam S7)
CMB phasic structure / sound cone as 2nd cone  spine §8   — bath mismatch (mirror seam C4)
cosmology as a traverse between the poles       spine §14  — depends on the unowned invariant
evaporation endgame / Planck granulation        spine §15  — off-map by its own admission
cosmological mirror (same wall, two sides)      mirror C1/C2 — trans-Planckian; strong form rejected
```

## The cross-cutting gate (the relabel test)

Even a fully graduated set of these contacts only **refines** PB-2 — it does not rescue it from
relabel. If the per-adjacency corners fire **and** no downstream bet forces a distinct route, the
fork reduces to relabel regardless of how elegant the fusion picture is (CLAIMS Tier B / D12). The
graduated core above was written to survive *before* any parked contact graduates, precisely so it
does not depend on the imports.
