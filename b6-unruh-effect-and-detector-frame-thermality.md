# B6 — Unruh Effect and Detector-Frame Thermality

Status: working contact / detector-response note.  
Grade: detector-response witness for the record/constitution split; quantitative uptake-temperature routing; bridge clarifier for B3/B4 horizon thermality; magnitude QFT-priced and framework-disowned.

This note tests a clean flat-spacetime case where thermality is not a globally intrinsic particle inventory. The Unruh effect says that an accelerated detector coupled to the Minkowski vacuum responds thermally with temperature

```text
T_U = ℏ a / (2π c k_B)
```

The framework-owned question is not whether it can derive the coefficient. It cannot and should not. The coefficient is QFT-owned. The framework-owned question is the role assignment:

```text
field state alone
versus
field state + detector worldline + flow-bearing uptake
```

Unruh is useful because it converts an apparently ontological question — "are there particles / a thermal bath?" — into a detector-response question. That is exactly the record/constitution seam.

## 1. Claim under audit

The possible mistaken reading is:

```text
accelerated observer sees thermality
therefore thermality is an intrinsic field substance
or constituted record in the vacuum itself
```

The framework asks instead:

```text
Where is the thermal record constituted?
```

Candidate role split:

```text
Minkowski vacuum:
  quantum field state;
  no inertial particle bath

accelerated worldline:
  non-geodesic flow-bearing sampling path

Rindler horizon:
  trajectory-relative accessibility / ordering boundary

Unruh-DeWitt detector response:
  flow-bearing uptake record

thermal temperature:
  response parameter externally priced by QFT
```

## 2. Role map

The Unruh composition is:

```text
field influence:
  the detector couples to field degrees of freedom

ordering boundary:
  accelerated motion introduces a Rindler wedge / horizon structure

flow:
  the detector has proper time and records response along its worldline

uptake:
  detector transitions constitute the thermal record
```

So the framework-native statement is:

```text
thermality = field influence + non-inertial ordering boundary + detector flow-bearing uptake
```

This does not mean the field has no structure. It means the constituted thermal record is not present without the detector/worldline sampling relation.

## 3. Quantitative output

B6 should be number-producing, not merely prohibitive.

The positive route is:

```text
acceleration in
→ detector temperature out
```

or:

```text
a → T_U
T_U = ℏ a / (2π c k_B)
```

Framework-owned:

```text
acceleration / Rindler horizon relation / detector flow
→ constituted thermal response
```

Externally owned:

```text
Unruh coefficient
field quantization
Rindler mode decomposition
Unruh-DeWitt detector response function
```

Therefore the quantitative prediction is not a new temperature law. It is a role-routing law:

```text
if thermality appears in this setup,
it appears as detector-response statistics along an accelerated flow-bearing worldline,
not as detector-independent constituted thermal substance in the inertial vacuum.
```

Compact:

```text
Unruh = acceleration-priced uptake thermality.
```

## 4. Record versus constitution

The record/constitution distinction from A3 applies directly.

```text
field state / vacuum correlations:
  record-capable structure
  not yet a constituted detector record

detector response:
  constituted record
  transition statistics in detector proper time
```

This avoids two bad readings.

Bad reading 1:

```text
the inertial vacuum literally contains a thermal particle bath
as an observer-independent constituted object.
```

Bad reading 2:

```text
there is no real effect because inertial observers see no particles.
```

Correct framework reading:

```text
The effect is real as detector response.
The thermal record is constituted at detector uptake.
The particle/thermal decomposition is trajectory-relative.
```

So Unruh supports the line:

```text
particle / thermal content is not always intrinsic to field state alone;
it can be a relation between field state, trajectory, and uptake.
```

## 5. Horizon role

The Rindler horizon is an accessibility boundary for the accelerated observer.

Framework assignment:

```text
Rindler horizon:
  ordering / accessibility boundary
  not a semantic container
  not a place where constituted information must live
```

This matches the B3/B4 horizon discipline:

```text
horizon / cut / boundary:
  access, accounting, area, mode split, wedge selection

constitution:
  detector response, reconstruction, measurement, uptake
```

So B6 reinforces the B3/B4 null-boundary rule without importing black-hole singularity or QES complications.

## 6. Relation to Hawking radiation

Unruh is a local / detector-response cousin of Hawking thermality, not a full replacement for it.

Useful comparison:

```text
Unruh:
  accelerated detector + Minkowski vacuum
  → thermal detector response

Hawking:
  black-hole spacetime + field vacuum + asymptotic observer
  → thermal flux at infinity
```

Framework bridge:

```text
both involve horizon/accessibility structure and observer/detector-relative mode decompositions;
both require care before assigning thermality to a boundary as constituted information.
```

Guardrail:

```text
Unruh thermality is detector-trajectory response in flat spacetime.
Hawking radiation is an asymptotic flux in black-hole spacetime.
Do not collapse them.
```

B6 can therefore clarify B4:

```text
Hawking thermality should not be read as "the horizon contains constituted information."
Unruh already shows thermality can arise from field state + worldline + detector response.
```

## 7. Relation to redshift

Unruh also parallels the redshift note.

Redshift:

```text
energy / frequency is projection at uptake:
E = -p · u
```

Unruh:

```text
thermal response is detector-worldline dependent:
T_U = ℏ a / (2π c k_B)
```

Shared framework point:

```text
what becomes physically consequential depends on the flow-bearing receiver / detector worldline.
```

This does not make the effect subjective in the weak sense. It makes it relational and operational.

## 8. Prediction bundle

The framework should state the Unruh predictions as role assignments and number-routing, not as a new derivation.

```text
1. Acceleration-priced thermality:
   a → T_U = ℏa/(2πck_B).

2. The thermal number is a detector-response number,
   not a detector-independent particle-substance number for the inertial vacuum.

3. The constituted thermal record occurs in detector uptake / transition statistics.

4. The Rindler horizon is an accessibility / ordering boundary,
   not a semantic container or constituted information site.

5. Changing the observer trajectory changes the particle/thermal decomposition
   without changing the global Minkowski vacuum state.

6. Field structure and vacuum correlations may be record-capable,
   but constituted thermal records require flow-bearing sampling.

7. B6 supports B3/B4 by separating horizon/accessibility structure
   from information constitution.
```

Compact null/positive form:

```text
ε_field-intrinsic-thermal-record = 0
```

Positive number-producing form:

```text
accelerated detector worldline in → Unruh temperature / response spectrum out
```

## 9. Could-have-failed

B6 would fail or weaken if:

```text
thermal particle content in the Unruh setup could be assigned as a detector-independent
constituted object in the inertial vacuum;
```

or if:

```text
the detector response could be fully explained without worldline acceleration,
Rindler wedge structure, or flow-bearing sampling;
```

or if:

```text
there were a convention-free global particle inventory that both inertial and accelerated
observers must share while still yielding the Unruh thermal response.
```

B6 would also weaken if the framework used Unruh to deny the physicality of detector clicks. That would be wrong. The detector response is physical. The point is the location of constitution.

## 10. Grade

```text
Field taxonomy:
  standard QFT-in-curved-spacetime / detector-response result
  no framework discovery of magnitude

Internal audit:
  strong
  cleanly separates field state, trajectory, horizon/accessibility, and detector uptake

Quantitative output:
  strong as externally priced number-routing
  a → T_U

Record/constitution relevance:
  strong
  thermality appears in detector response, not as field-state-alone constituted record

B3/B4 bridge:
  strong clarifier
  horizon/accessibility structure is not automatically information constitution

Collision-room:
  global detector-independent constituted thermal bath in Minkowski vacuum would break the role assignment
  denying detector-response physicality would also break the note
```

## 11. Compact result

Unruh is the cleanest flat-spacetime test of detector-frame thermality.

```text
Minkowski vacuum:
  no inertial thermal particle bath

accelerated detector:
  thermal response

temperature:
  T_U = ℏa/(2πck_B)

constitution site:
  detector uptake along a flow-bearing worldline
```

Therefore:

```text
thermality is not always an intrinsic particle inventory of the field alone.
In the Unruh setup, thermality is constituted as detector response under accelerated sampling.
```

Framework-native slogan:

```text
Unruh = acceleration-priced uptake thermality.
```

## References / external anchors

- Unruh effect: uniformly accelerated detector in Minkowski vacuum responds thermally.
- Unruh-DeWitt detector model and response functions.
- Rindler coordinates, Rindler wedges, and observer-relative horizons.
- Bogoliubov transformations between Minkowski and Rindler modes.
- Hawking-Unruh relation and QFT in curved spacetime.
- Detector-based operational treatments of particle content.
