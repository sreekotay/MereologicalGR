# A1 Refactor Around A0

*Sree Kotay — working note, June 2026.*

Status: non-destructive refactor note.  
Purpose: specify the A1 update after A0 now owns the foundation grammar and photon seed.

A0 now carries:

```text
GR-held-fixed stance
photon seed / cause-without-time defusion
method / ledger / grading grammar
component roster
composition grammar
foundation-form PB bets
rendering / uptake seam
anti-over-unfusing rule
```

Therefore A1 should no longer carry the full foundation burden. A1 should become the extended run and cashing document:

```text
A1 = full photon-seed run + downstream checks + PB status/cashing + corrections
```

---

## Replacement opening for A1

Suggested title:

```text
# What Could "Cause Without Time" Mean? — The Photon Run of MGR
```

Suggested companion line:

```text
*Foundation document: `A0-Mereological-Foundations-for-General-Relativity.md` states the GR-held-fixed stance, photon seed, role grammar, ledger discipline, and foundation-form PB bets. This document runs that foundation from the photon seed and tracks the witness/contact/correction history.*
```

Suggested abstract:

```text
A0 states the MGR foundation: GR held fixed; the photon seed; the role roster; the composition grammar; and the foundation-form PB bets. A1 is the first extended run of that foundation. It begins from the photon under GR: null propagation strips proper-time flow while preserving ordering and influence. That seed gives the cause-without-time decomposition — cause = ordering + influence, time = ordering + flow — and launches the downstream projections. A1 then tracks what the photon seed forces, what it merely suggests, where the projections land, how PB-1 through PB-4 are cashed or left open, and which checks fail, demote, or correct the framework. A1 is therefore not the foundation grammar itself; it is the photon seed's career under the A0 ledger.
```

Suggested conceit:

```text
The conceit is no longer introduced here from scratch. A0 states the theory. A1 applies it. The photon is both foundation seed and first run-site: at the null corner, GR itself separates ordering and influence from proper-time flow. A1 follows that separation outward through the phenomenon runs and standing corrections.
```

---

## A1 section-role map

A1 should keep the following as body material:

```text
photon as seed/exhibit in full detail
cause-without-time derivation as performed
component discussion where it adds seed-specific lineage
composition development where it cashes consequences
write-act application history
corner analyses
contact analyses
PB evidence/status/cashing
standing notes
corrections log
references
```

A1 should shorten or import-by-reference:

```text
full GR-held-fixed stance
full compose/missing/extra method
full forced/chosen/imported/parked rule
full epistemic grading list
full component roster as a canonical foundation table
foundation-form PB statements
anti-over-unfusing rule
```

Those now belong to A0.

---

## Minimal structural change to A1

Replace the current front matter through `### 1.6 Structure of the paper` with a shorter import-and-run opening:

```text
## Abstract

A0 states the MGR foundation: GR held fixed, the photon seed, the role grammar, the ledger discipline, and the foundation-form PB bets. This document is the full photon run. It begins from the null corner — dτ = 0 while ordering and influence remain — and follows the resulting cause-without-time decomposition through the downstream checks. A1's job is not to restate the entire foundation; it is to cash, stress, demote, and correct the projections launched by the seed.

## 1. Introduction

### 1.1 What this document does

A0 owns the foundation grammar. A1 runs it. The photon seed is stated in A0 because it motivates the whole defusion; A1 keeps the detailed photon analysis and the downstream career of that seed.

### 1.2 The photon run

Under GR held fixed, null propagation strips proper-time flow while preserving cone-direction and effect-capacity. The full body below works out the consequences: cause = ordering + influence; time = ordering + flow; information requires ordering + influence + flow; photons therefore carry influence-structure, not constituted information, unless PB-3 fails by a completed information-grade operation with no flow-bearing uptake.

### 1.3 How to read grades here

Grades and ledger rules are inherited from A0. A1 uses them in application: seed/exhibit, witness, probe, clarifier, failed floor, parked texture, and correction. A1's statuses are local to this photon run unless promoted back into A0.

### 1.4 Structure of the run

§2 retains the component discussion as applied to the photon lineage. §3 develops the compositions. §4 and onward run the write-act, corner analyses, contact analyses, PB cashing, standing notes, and corrections. The corrections log is part of the result: a framework that cannot lose is not making claims.
```

---

## Notes on preservation

Do **not** delete the long A1 material merely because A0 exists. Much of it is not foundation duplication; it is application history.

Preserve especially:

```text
Influence floor detail
mass as flow-projection development
standing notes calibration
PB cashing/status details
photonic-success shield
failed floors and corrections
Unruh regrade history
Levi-Civita correction history
Casimir failed-floor entry
```

The target is not shorter A1 by default. The target is cleaner ownership:

```text
A0 = foundation grammar + seed + foundation bets
A1 = the seed's full run and scar tissue
```

---

## Safe-edit rule

Because A1 is long and contains valuable correction history, update it only with a full-preservation pass. Do not replace the main A1 file from a truncated fetch. If tool access cannot preserve the entire file exactly, keep this note as the staging document and apply the front-matter refactor when a safe full-file edit is available.